{
    Print(9);
    Print("Hello, world!");
    Print(true);
    Print(6.9);
    Print('f');
    //Print('abc') // Should be invalid
    Print();

    Print(!false && true);
    Print(!(false||false));
    Print(2+2*2);
    Print((2+2)*2%3);
    Print("ab"."cd");
    Print(2%0.5);
    Print();

    string a = "hello";
    Print(a);
    float b = 6.9;
    Print(b);
    Print();

    var c = 5;
    Print(c);
    c =
        c * 2;
    Print(c);
    //c = "text" //Invalid because of type mismatch
    Print();

    { //Standalone block
        Print("Enter some text");
        string text = Read(); //returns string type text
        Print(text);
        Print();
        c = 6;
        {
            c = 5;
        }
    }

    //Print(text); //Invalid because variable text is local to the block
    Print(c);// Prints 7 because it was changed inside of block

    if(c>6) {
        Print("if");
        Print(c<=>7);//Spaceship operator
    }
    else if (c < 4)
    {
        Print("else if");
        Print(c!=3);
    }
    else{
    Print("else");
    Print(c<=5);
    }

    Print(1.41421356237**2);
    Print();

    int fileInt = fetch "code\DataFile.fdat" line 1 field 1 delimedBy "abc";
    Print(fileInt);
    string fileString = fetch "code\DataFile.fdat" line 1 field 3 delimedBy "abc";
    Print(fileString);
    float fileFloat = fetch "code\DataFile.fdat" line 2 field 3 delimedBy ";;";
    Print(fileFloat);
    Print();

    string fileLine = fetch "code\DataFile.fdat" line 2;
    Print(fileLine);
    Print();

    string fileText = fetch "code\DataFile.fdat";
    Print(fileText);
    Print();

    Save("code\SavedData.fdat");
}
Save("code\BeforeLoad.fdat");
Load("code\SavedData.fdat");
Save("code\AfterLoad.fdat");
Print("After Load:");
Print(fileText);

Print("");
Print("Program to print nth fibonacci:");
int n = 10;
int first = 1;
int second = 1;
Print(1);
while(n > 0) {
 int oldSecond = second;
 second = second + first;
 first = oldSecond;
 Print(second);
 n = n - 1;
}